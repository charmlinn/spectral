export const VIDEO_PROJECT_SCHEMA_VERSION = 1;

export const DEFAULT_PROJECT_DIMENSIONS = {
  width: 1080,
  height: 1080,
} as const;

export const DEFAULT_EXPORT_DIMENSIONS = {
  width: 1080,
  height: 1080,
} as const;

export const DEFAULT_FRAME_RATE = 60;

export const DEFAULT_PROJECT_DURATION_MS = 30_000;

export const LEGACY_SPECTERR_PRESET_VERSION = 16;
