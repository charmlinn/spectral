Copied from `/Users/linncharm/Project/specterr/GetPresets.json` to satisfy the project copy-first rule.

The importer keeps the original payload in the `presets.sourcePayload` column and derives a normalized `VideoProject` into `presets.projectData`.
